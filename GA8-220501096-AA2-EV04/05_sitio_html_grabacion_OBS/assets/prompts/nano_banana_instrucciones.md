# Instrucciones para Nano Banana Pro

Pega el archivo JSON `nano_banana_pro_secuencial.json`. Genera la imagen 1. Cuando termine, escribe `avanza` para continuar una por una. Guarda cada imagen en `assets/generated` con el nombre exacto indicado, por ejemplo `01_datos.png`.
