export function NavigationBar(activeRoute="home"){const items=[["home","Inicio"],["products","Productos"],["order","Crear pedido"],["kitchen","Cocina"],["admin","Administración"],["about","Acerca de"]];return`<nav class="nav-bar">${items.map(([route,label])=>`<button class="${activeRoute===route?"active":""}" data-route="${route}" type="button">${label}</button>`).join("")}</nav>`;}
