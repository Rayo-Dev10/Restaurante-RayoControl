import { NavigationBar } from "../components/NavigationBar.js"; import { OrderSummary } from "../components/OrderSummary.js"; export function OrderView(products = [], currentOrder = null, errorMessage = "") { const options = products.filter((product) => product.disponible).map((product) => `<option value="${product.idProducto}">${product.nombre} - ${product.precio}</option>`).join(""); return `<main class="app-shell"><section class="panel"><p class="eyebrow">Módulo cliente · Pedido</p><h1>Crear pedido</h1><p class="subtitle">Selecciona producto, cantidad, cliente o mesa para generar el pedido.</p>${NavigationBar("order")}<form id="create-order-form" class="order-form"><label>Cliente o referencia<input name="cliente" value="Cliente móvil EV02" required></label><label>Mesa<input name="mesa" value="1"></label><label>Producto<select name="productId">${options}</select></label><label>Cantidad<input name="cantidad" type="number" min="1" value="2" required></label><button type="submit">Crear pedido</button></form>${errorMessage ? `<p class="error-message">${errorMessage}</p>` : ""}${OrderSummary(currentOrder)}</section></main>`; }
