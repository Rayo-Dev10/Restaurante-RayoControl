import{formatCurrency}from"../services/orderService.js";export function ProductCard(product){const estado=product.disponible?"Disponible":"No disponible";return`<article class="product-card"><div><span class="tag">${product.categoria}</span><h3>${product.nombre}</h3><p>${product.descripcion}</p></div><div class="product-card__footer"><strong>${formatCurrency(product.precio)}</strong><small>${estado}</small></div></article>`;}
