import{calculateSubtotal,calculateTotal,formatCurrency}from"../services/orderService.js";import{StatusBadge}from"./StatusBadge.js";export function OrderSummary(order){if(!order){return`<section class="empty-state"><h3>Sin pedido activo</h3><p>Crea un pedido para visualizar el resumen.</p></section>`;}const rows=(order.items||[]).map((item)=>`<tr><td>${item.nombre}</td><td>${item.cantidad}</td><td>${formatCurrency(item.precio)}</td><td>${formatCurrency(calculateSubtotal(item))}</td></tr>`).join("");return`<section class="summary-card"><div class="summary-card__header"><div><span class="tag">Pedido ${order.idPedido}</span><h3>${order.cliente}</h3><p>Mesa: ${order.mesa} · ${order.fechaHora}</p></div>${StatusBadge(order.estado)}</div><table><thead><tr><th>Producto</th><th>Cant.</th><th>Valor</th><th>Subtotal</th></tr></thead><tbody>${rows}</tbody></table><div class="total-line"><span>Total</span><strong>${formatCurrency(order.total||calculateTotal(order.items||[]))}</strong></div></section>`;}
