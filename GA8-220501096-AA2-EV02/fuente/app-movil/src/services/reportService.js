import{calculateTotal}from"./orderService.js";export const countAvailableProducts=(products=[])=>products.filter((product)=>product.disponible).length;export const countOrders=(orders=[])=>orders.length;export const sumOrdersTotal=(orders=[])=>orders.reduce((total,order)=>total+calculateTotal(order.items||[]),0);export const countOrdersByStatus=(orders=[],status)=>orders.filter((order)=>order.estado===status).length;
