export const formatCurrency=(value)=>new Intl.NumberFormat("es-CO",{style:"currency",currency:"COP",maximumFractionDigits:0}).format(Number(value)||0);export const validateQuantity=(quantity)=>Number.isInteger(Number(quantity))&&Number(quantity)>=1;export const calculateSubtotal=(item)=>Number(item.precio)*Number(item.cantidad);export const calculateTotal=(items=[])=>items.reduce((total,item)=>total+calculateSubtotal(item),0);export function createOrder({cliente,mesa,product,cantidad}){if(!cliente||!String(cliente).trim()){throw new Error("El cliente o referencia es obligatorio.");}if(!product){throw new Error("Debe seleccionar un producto.");}if(!product.disponible){throw new Error("El producto seleccionado no está disponible.");}if(!validateQuantity(cantidad)){throw new Error("La cantidad debe ser mayor o igual a 1.");}const item={idProducto:product.idProducto,nombre:product.nombre,precio:product.precio,cantidad:Number(cantidad)};return{idPedido:Date.now(),cliente:String(cliente).trim(),mesa:mesa&&String(mesa).trim()?String(mesa).trim():"Sin mesa",estado:"RECIBIDO",fechaHora:new Date().toLocaleString("es-CO"),items:[item],total:calculateTotal([item])};}export function changeOrderStatus(order,status){return{...order,estado:status};}
