import{describe,it,expect}from"vitest";import{countAvailableProducts,countOrders,sumOrdersTotal,countOrdersByStatus}from"../services/reportService.js";const products=[{disponible:true},{disponible:false},{disponible:true}];const orders=[{estado:"RECIBIDO",items:[{precio:10000,cantidad:2}]},{estado:"LISTO",items:[{precio:5000,cantidad:3}]}];describe("reportService",()=>{it("cuenta productos disponibles",()=>{expect(countAvailableProducts(products)).toBe(2);});it("cuenta pedidos registrados",()=>{expect(countOrders(orders)).toBe(2);});it("suma el total de pedidos",()=>{expect(sumOrdersTotal(orders)).toBe(35000);});it("cuenta pedidos por estado",()=>{expect(countOrdersByStatus(orders,"LISTO")).toBe(1);});});
