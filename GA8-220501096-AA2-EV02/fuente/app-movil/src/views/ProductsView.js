import{NavigationBar}from"../components/NavigationBar.js";import{ProductCard}from"../components/ProductCard.js";export function ProductsView(products=[]){return`<main class="app-shell"><section class="panel"><p class="eyebrow">Módulo cliente · Producto</p><h1>Menú disponible</h1><p class="subtitle">Productos locales usados para simular el flujo móvil de pedidos.</p>${NavigationBar("products")}<div class="product-grid">${products.map(ProductCard).join("")}</div></section></main>`;}
